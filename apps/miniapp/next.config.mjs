/** @type {import('next').NextConfig} */
const config = {
  allowedDevOrigins: ["127.0.0.1"],
  devIndicators: {
    position: "top-right"
  },
  reactStrictMode: true,
  typedRoutes: true
};

export default config;
