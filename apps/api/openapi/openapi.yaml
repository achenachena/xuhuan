openapi: 3.1.0
info:
  title: Xuhuan Game API
  version: 1.0.0
  summary: Server-authoritative API for the Xuhuan Telegram Mini App
  description: >-
    The API owns player identity, battle state, randomness, progression, and rewards.
    DevelopmentAuth is accepted only by an explicitly enabled development process and
    is never available in production.
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
servers:
  - url: http://localhost:8080
    description: Local development
  - url: https://api.example.com
    description: Production placeholder
tags:
  - name: Platform
  - name: Player
  - name: Catalog
  - name: Battle
  - name: Game V2
  - name: Story V2
paths:
  /healthz:
    get:
      operationId: getHealth
      tags: [Platform]
      summary: Process liveness
      security: []
      responses:
        "200":
          description: Process is alive
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
  /readyz:
    get:
      operationId: getReadiness
      tags: [Platform]
      summary: Required dependency readiness
      security: []
      responses:
        "200":
          description: API is ready to serve traffic
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Readiness"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /v1/player:
    get:
      operationId: getPlayer
      tags: [Player]
      summary: Get or create the authenticated player
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      responses:
        "200":
          description: Current player profile
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Player"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/characters:
    get:
      operationId: listCharacters
      tags: [Catalog]
      summary: List playable characters
      security: []
      responses:
        "200":
          description: Active character catalog
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [characters]
                properties:
                  characters:
                    type: array
                    items:
                      $ref: "#/components/schemas/Character"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/characters/{slug}:
    parameters:
      - $ref: "#/components/parameters/Slug"
    get:
      operationId: getCharacter
      tags: [Catalog]
      summary: Get one playable character
      security: []
      responses:
        "200":
          description: Character detail
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Character"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/encounters:
    get:
      operationId: listEncounters
      tags: [Catalog]
      summary: List active encounters
      security: []
      responses:
        "200":
          description: Active encounter catalog
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [encounters]
                properties:
                  encounters:
                    type: array
                    items:
                      $ref: "#/components/schemas/Encounter"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/encounters/{slug}:
    parameters:
      - $ref: "#/components/parameters/Slug"
    get:
      operationId: getEncounter
      tags: [Catalog]
      summary: Get one active encounter
      security: []
      responses:
        "200":
          description: Encounter detail
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Encounter"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/battles:
    post:
      operationId: createBattle
      tags: [Battle]
      summary: Create a server-seeded battle and debit its energy cost
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBattleRequest"
      responses:
        "201":
          description: Battle created
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            Idempotency-Replayed:
              $ref: "#/components/headers/IdempotencyReplayed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Battle"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/battles/{id}:
    parameters:
      - $ref: "#/components/parameters/BattleID"
    get:
      operationId: getBattle
      tags: [Battle]
      summary: Get the player's authoritative battle state
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      responses:
        "200":
          description: Current battle state
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Battle"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v1/battles/{id}/actions:
    parameters:
      - $ref: "#/components/parameters/BattleID"
    post:
      operationId: createBattleAction
      tags: [Battle]
      summary: Apply one authoritative, versioned battle action
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BattleActionRequest"
      responses:
        "200":
          description: Action result and resulting battle
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            Idempotency-Replayed:
              $ref: "#/components/headers/IdempotencyReplayed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleActionResponse"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
  /v2/content/{version}:
    parameters:
      - name: version
        in: path
        required: true
        schema:
          type: string
          pattern: "^v[1-9][0-9]*$"
      - name: locale
        in: query
        required: false
        schema:
          type: string
          enum: [zh-CN, en]
    get:
      operationId: getGameContent
      tags: [Game V2]
      summary: Get one immutable localized roguelite content bundle
      security: []
      responses:
        "200":
          description: Localized game content
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            ETag:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GameContent"
        "304":
          description: Cached content is current
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /v2/game:
    get:
      operationId: getGameState
      tags: [Game V2]
      summary: Get player story progress and resumable run
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      responses:
        "200":
          description: Current game state
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GameSnapshot"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
  /v2/runs:
    post:
      operationId: createRun
      tags: [Game V2]
      summary: Start a server-seeded roguelite run
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRunRequest"
      responses:
        "201":
          description: Run created
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            Idempotency-Replayed:
              $ref: "#/components/headers/IdempotencyReplayed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GameRun"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
  /v2/runs/{id}:
    parameters:
      - $ref: "#/components/parameters/BattleID"
    get:
      operationId: getRun
      tags: [Game V2]
      summary: Resume one player-owned run
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      responses:
        "200":
          description: Authoritative run state
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GameRun"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
  /v2/runs/{id}/commands:
    parameters:
      - $ref: "#/components/parameters/BattleID"
    post:
      operationId: createRunCommand
      tags: [Game V2]
      summary: Apply one authoritative versioned run command
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RunCommandRequest"
      responses:
        "200":
          description: Resulting run and ordered presentation events
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            Idempotency-Replayed:
              $ref: "#/components/headers/IdempotencyReplayed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunCommandResponse"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
  /v2/story/choices:
    post:
      operationId: createStoryChoice
      tags: [Story V2]
      summary: Resolve the currently pending authored story scene
      security:
        - TelegramInitData: []
        - DevelopmentAuth: []
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryChoiceRequest"
      responses:
        "200":
          description: Updated story progress
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestID"
            Idempotency-Replayed:
              $ref: "#/components/headers/IdempotencyReplayed"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryChoiceResponse"
        "400":
          $ref: "#/components/responses/InvalidRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
components:
  securitySchemes:
    TelegramInitData:
      type: apiKey
      in: header
      name: X-Telegram-Init-Data
      description: Raw, unmodified Telegram Mini App initData
    DevelopmentAuth:
      type: apiKey
      in: header
      name: X-Dev-Auth
      description: Explicit local token; impossible to enable when APP_ENV is production
  headers:
    RequestID:
      description: Correlation ID generated or accepted by the API
      schema:
        type: string
        minLength: 1
        maxLength: 128
    IdempotencyReplayed:
      description: True when the exact stored response was replayed
      schema:
        type: boolean
  parameters:
    Slug:
      name: slug
      in: path
      required: true
      schema:
        type: string
        pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
        minLength: 1
        maxLength: 64
    BattleID:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: Unique per player and operation; reuse replays the original response
      schema:
        type: string
        minLength: 8
        maxLength: 128
        pattern: "^[A-Za-z0-9._:-]+$"
  responses:
    InvalidRequest:
      description: Request validation failed
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error:
              code: invalid_request
              message: The request is invalid
              request_id: 01J4Z6PQZ91GF4G74AM2R3PE8W
    Unauthorized:
      description: Authentication is missing, invalid, malformed, or stale
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error:
              code: unauthorized
              message: Authentication is required
              request_id: 01J4Z6PQZ91GF4G74AM2R3PE8W
    Forbidden:
      description: Authenticated player has not unlocked the requested content
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    NotFound:
      description: Resource is absent or not owned by the authenticated player
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error:
              code: not_found
              message: The requested resource was not found
              request_id: 01J4Z6PQZ91GF4G74AM2R3PE8W
    Conflict:
      description: Expected version, battle state, energy, or idempotency payload conflict
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          example:
            error:
              code: version_conflict
              message: The battle state has changed
              request_id: 01J4Z6PQZ91GF4G74AM2R3PE8W
    PayloadTooLarge:
      description: Request body exceeds the configured limit
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    UnsupportedMediaType:
      description: JSON endpoint did not receive application/json
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    RateLimited:
      description: Per-player or per-IP rate limit exceeded
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        Retry-After:
          schema:
            type: integer
            minimum: 1
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    InternalError:
      description: Unexpected internal failure
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    ServiceUnavailable:
      description: Required dependency is not ready
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    Health:
      type: object
      additionalProperties: false
      required: [status, version]
      properties:
        status:
          const: ok
        version:
          type: string
    Readiness:
      type: object
      additionalProperties: false
      required: [status, checks]
      properties:
        status:
          const: ready
        checks:
          type: object
          additionalProperties: false
          required: [postgres]
          properties:
            postgres:
              const: ready
    ErrorEnvelope:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: object
          additionalProperties: false
          required: [code, message, request_id]
          properties:
            code:
              type: string
              pattern: "^[a-z][a-z0-9_]*$"
            message:
              type: string
              minLength: 1
            request_id:
              type: string
              minLength: 1
    Player:
      type: object
      additionalProperties: false
      required: [id, telegram_user_id, username, display_name, level, experience, credits, energy, version, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        telegram_user_id:
          type: string
          pattern: "^[0-9]+$"
          description: String encoding avoids JavaScript integer precision loss
        username:
          type: [string, "null"]
        display_name:
          type: string
        level:
          type: integer
          minimum: 1
        experience:
          type: integer
          minimum: 0
        credits:
          type: integer
          minimum: 0
        energy:
          type: integer
          minimum: 0
          maximum: 180
        version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Character:
      type: object
      additionalProperties: false
      required:
        - id
        - slug
        - name
        - biography
        - archetype
        - base_health
        - base_attack
        - base_defense
        - base_speed
        - base_crit_rate
        - base_crit_damage
        - special_move_name
        - special_move_description
        - special_move_type
        - rarity
        - color_theme
        - portrait_url
        - model_url
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
        name:
          type: string
        biography:
          type: string
        archetype:
          type: string
        base_health:
          type: integer
          minimum: 1
        base_attack:
          type: integer
          minimum: 1
        base_defense:
          type: integer
          minimum: 0
        base_speed:
          type: integer
          minimum: 1
        base_crit_rate:
          type: number
          minimum: 0
          maximum: 1
        base_crit_damage:
          type: number
          minimum: 0
        special_move_name:
          type: string
        special_move_description:
          type: string
        special_move_type:
          type: string
        rarity:
          type: string
          enum: [common, rare, epic, legendary]
        color_theme:
          type: string
          pattern: "^#[0-9A-Fa-f]{6}$"
        portrait_url:
          type: string
          format: uri
        model_url:
          type: string
          format: uri
    Encounter:
      type: object
      additionalProperties: false
      required:
        - id
        - slug
        - name
        - description
        - level
        - max_health
        - attack
        - defense
        - speed
        - crit_rate
        - crit_damage
        - special_move_name
        - special_move_description
        - color_theme
        - image_url
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        level:
          type: integer
          minimum: 1
        max_health:
          type: integer
          minimum: 1
        attack:
          type: integer
          minimum: 1
        defense:
          type: integer
          minimum: 0
        speed:
          type: integer
          minimum: 1
        crit_rate:
          type: number
          minimum: 0
          maximum: 1
        crit_damage:
          type: number
          minimum: 0
        special_move_name:
          type: string
        special_move_description:
          type: string
        color_theme:
          type: string
          pattern: "^#[0-9A-Fa-f]{6}$"
        image_url:
          type: [string, "null"]
          format: uri
    CreateBattleRequest:
      type: object
      additionalProperties: false
      required: [character_slug, encounter_slug]
      properties:
        character_slug:
          type: string
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          maxLength: 64
        encounter_slug:
          type: string
          pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$"
          maxLength: 64
    BattleActionRequest:
      type: object
      additionalProperties: false
      required: [action, expected_version]
      properties:
        action:
          $ref: "#/components/schemas/ActionKind"
        expected_version:
          type: integer
          minimum: 1
    ActionKind:
      type: string
      enum: [light_attack, heavy_attack, special_move, block, counter]
    BattleStatus:
      type: string
      enum: [active, completed, cancelled]
    BattleOutcome:
      type: [string, "null"]
      enum: [victory, defeat, null]
    CombatantState:
      type: object
      additionalProperties: false
      required:
        - slug
        - name
        - level
        - max_health
        - current_health
        - attack
        - defense
        - speed
        - crit_rate
        - crit_damage
        - special_meter
        - combo_count
        - is_blocking
      properties:
        slug:
          type: string
        name:
          type: string
        level:
          type: integer
          minimum: 1
        max_health:
          type: integer
          minimum: 1
        current_health:
          type: integer
          minimum: 0
        attack:
          type: integer
          minimum: 1
        defense:
          type: integer
          minimum: 0
        speed:
          type: integer
          minimum: 1
        crit_rate:
          type: number
          minimum: 0
          maximum: 1
        crit_damage:
          type: number
          minimum: 0
        special_meter:
          type: integer
          minimum: 0
          maximum: 100
        combo_count:
          type: integer
          minimum: 0
        is_blocking:
          type: boolean
    Reward:
      type: object
      additionalProperties: false
      required: [experience, credits, energy]
      properties:
        experience:
          type: integer
          minimum: 0
        credits:
          type: integer
          minimum: 0
        energy:
          type: integer
          maximum: 0
    Battle:
      type: object
      additionalProperties: false
      required:
        - id
        - status
        - outcome
        - version
        - turn
        - character
        - encounter
        - hero
        - enemy
        - rewards
        - created_at
        - updated_at
        - completed_at
      properties:
        id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/BattleStatus"
        outcome:
          $ref: "#/components/schemas/BattleOutcome"
        version:
          type: integer
          minimum: 1
        turn:
          type: integer
          minimum: 1
        character:
          $ref: "#/components/schemas/Character"
        encounter:
          $ref: "#/components/schemas/Encounter"
        hero:
          $ref: "#/components/schemas/CombatantState"
        enemy:
          $ref: "#/components/schemas/CombatantState"
        rewards:
          oneOf:
            - $ref: "#/components/schemas/Reward"
            - type: "null"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        completed_at:
          type: [string, "null"]
          format: date-time
    DamageResult:
      type: object
      additionalProperties: false
      required: [amount, critical, blocked, countered]
      properties:
        amount:
          type: integer
          minimum: 0
        critical:
          type: boolean
        blocked:
          type: boolean
        countered:
          type: boolean
    ActionEvent:
      type: object
      additionalProperties: false
      required: [actor, action, damage, description]
      properties:
        actor:
          type: string
          enum: [hero, enemy]
        action:
          $ref: "#/components/schemas/ActionKind"
        damage:
          oneOf:
            - $ref: "#/components/schemas/DamageResult"
            - type: "null"
        description:
          type: string
    BattleActionResult:
      type: object
      additionalProperties: false
      required: [sequence, events]
      properties:
        sequence:
          type: integer
          minimum: 1
        events:
          type: array
          minItems: 1
          maxItems: 2
          items:
            $ref: "#/components/schemas/ActionEvent"
    BattleActionResponse:
      type: object
      additionalProperties: false
      required: [battle, result]
      properties:
        battle:
          $ref: "#/components/schemas/Battle"
        result:
          $ref: "#/components/schemas/BattleActionResult"
    GameEffect:
      type: object
      additionalProperties: false
      required: [kind]
      properties:
        kind:
          type: string
        amount:
          type: integer
        status:
          type: string
    GameCharacterContent:
      type: object
      additionalProperties: false
      required: [slug, name, biography, playstyle, color_theme, portrait_url, model_url, available]
      properties:
        slug:
          type: string
        name:
          type: string
        biography:
          type: string
        playstyle:
          type: string
        color_theme:
          type: string
        portrait_url:
          type: string
          format: uri
        model_url:
          type: string
          format: uri
        available:
          type: boolean
    GameCardContent:
      type: object
      additionalProperties: false
      required: [slug, character_slug, name, description, type, target, rarity, cost, starter_copies, exhaust, unplayable, effects]
      properties:
        slug:
          type: string
        character_slug:
          type: [string, "null"]
        name:
          type: string
        description:
          type: string
        type:
          type: string
          enum: [attack, defense, signal, glitch]
        target:
          type: string
          enum: [none, self, enemy, all_enemies]
        rarity:
          type: string
        cost:
          type: integer
          minimum: 0
        starter_copies:
          type: integer
          minimum: 0
        exhaust:
          type: boolean
        unplayable:
          type: boolean
        effects:
          type: array
          items:
            $ref: "#/components/schemas/GameEffect"
    EnemyIntentContent:
      type: object
      additionalProperties: false
      required: [slug, name, description, effects]
      properties:
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        effects:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/GameEffect"
    GameEnemyContent:
      type: object
      additionalProperties: false
      required: [slug, name, description, kind, max_health, color_theme, image_url, intents]
      properties:
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        kind:
          type: string
          enum: [normal, elite, boss]
        max_health:
          type: integer
          minimum: 1
        color_theme:
          type: string
        image_url:
          type: string
        intents:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/EnemyIntentContent"
    GameRelicContent:
      type: object
      additionalProperties: false
      required: [slug, name, description, effect]
      properties:
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        effect:
          $ref: "#/components/schemas/GameEffect"
    GameEventOptionContent:
      type: object
      additionalProperties: false
      required: [slug, label, result]
      properties:
        slug:
          type: string
        label:
          type: string
        result:
          type: string
    GameEventContent:
      type: object
      additionalProperties: false
      required: [slug, title, body, options]
      properties:
        slug:
          type: string
        title:
          type: string
        body:
          type: string
        options:
          type: array
          minItems: 2
          items:
            $ref: "#/components/schemas/GameEventOptionContent"
    StoryMessageContent:
      type: object
      additionalProperties: false
      required: [sender, kind, text]
      properties:
        sender:
          type: string
        kind:
          type: string
          enum: [system, character]
        text:
          type: string
    StoryOptionContent:
      type: object
      additionalProperties: false
      required: [slug, label]
      properties:
        slug:
          type: string
        label:
          type: string
    StorySceneContent:
      type: object
      additionalProperties: false
      required: [slug, title, messages, options]
      properties:
        slug:
          type: string
        title:
          type: string
        messages:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/StoryMessageContent"
        options:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/StoryOptionContent"
    ChapterContent:
      type: object
      additionalProperties: false
      required: [slug, title, subtitle, character_slug, available]
      properties:
        slug:
          type: string
        title:
          type: string
        subtitle:
          type: string
        character_slug:
          type: string
        available:
          type: boolean
    GameContent:
      type: object
      additionalProperties: false
      required: [version, locale, characters, cards, enemies, relics, events, scenes, chapters]
      properties:
        version:
          type: string
        locale:
          type: string
          enum: [zh-CN, en]
        characters:
          type: array
          items:
            $ref: "#/components/schemas/GameCharacterContent"
        cards:
          type: array
          items:
            $ref: "#/components/schemas/GameCardContent"
        enemies:
          type: array
          items:
            $ref: "#/components/schemas/GameEnemyContent"
        relics:
          type: array
          items:
            $ref: "#/components/schemas/GameRelicContent"
        events:
          type: array
          items:
            $ref: "#/components/schemas/GameEventContent"
        scenes:
          type: array
          items:
            $ref: "#/components/schemas/StorySceneContent"
        chapters:
          type: array
          items:
            $ref: "#/components/schemas/ChapterContent"
    PlayerUnlock:
      type: object
      additionalProperties: false
      required: [type, content_slug, created_at]
      properties:
        type:
          type: string
          enum: [character, card, relic, starter_module]
        content_slug:
          type: string
        created_at:
          type: string
          format: date-time
    StoryChoice:
      type: object
      additionalProperties: false
      required: [scene_slug, option_slug, choice_tag, created_at]
      properties:
        scene_slug:
          type: string
        option_slug:
          type: string
        choice_tag:
          type: string
        created_at:
          type: string
          format: date-time
    GameProgress:
      type: object
      additionalProperties: false
      required: [current_chapter_slug, highest_noise_level, story_version, version, unlocks, choices]
      properties:
        current_chapter_slug:
          type: string
        highest_noise_level:
          type: integer
          minimum: 0
          maximum: 10
        story_version:
          type: integer
          minimum: 1
        version:
          type: integer
          minimum: 1
        unlocks:
          type: array
          items:
            $ref: "#/components/schemas/PlayerUnlock"
        choices:
          type: array
          items:
            $ref: "#/components/schemas/StoryChoice"
    GamePlayer:
      type: object
      additionalProperties: false
      required: [id, display_name, language_code]
      properties:
        id:
          type: string
          format: uuid
        display_name:
          type: string
        language_code:
          type: [string, "null"]
    CardInstance:
      type: object
      additionalProperties: false
      required: [id, slug]
      properties:
        id:
          type: string
        slug:
          type: string
    CombatPlayerState:
      type: object
      additionalProperties: false
      required: [max_health, health, block, bandwidth, next_bandwidth, distortion, distortion_limit, beacons, weak, vulnerable, discount_signal, distortion_shield_used, first_attack_bonus_used]
      properties:
        max_health: {type: integer, minimum: 1}
        health: {type: integer, minimum: 0}
        block: {type: integer, minimum: 0}
        bandwidth: {type: integer, minimum: 0}
        next_bandwidth: {type: integer}
        distortion: {type: integer, minimum: 0}
        distortion_limit: {type: integer, minimum: 1}
        beacons: {type: integer, minimum: 0, maximum: 3}
        weak: {type: integer, minimum: 0}
        vulnerable: {type: integer, minimum: 0}
        discount_signal: {type: integer, minimum: 0}
        distortion_shield_used: {type: boolean}
        first_attack_bonus_used: {type: boolean}
    CombatEnemyState:
      type: object
      additionalProperties: false
      required: [id, slug, max_health, health, block, strength, weak, vulnerable, intent_index]
      properties:
        id: {type: string}
        slug: {type: string}
        max_health: {type: integer, minimum: 1}
        health: {type: integer, minimum: 0}
        block: {type: integer, minimum: 0}
        strength: {type: integer, minimum: 0}
        weak: {type: integer, minimum: 0}
        vulnerable: {type: integer, minimum: 0}
        intent_index: {type: integer, minimum: 0}
    CombatStateV2:
      type: object
      additionalProperties: false
      required: [status, turn, seed, rng_cursor, player, enemies, draw_pile, discard_pile, hand, exhaust_pile, played_types, cards_played, route_completed, next_card_sequence, noise_level]
      properties:
        status:
          type: string
          enum: [active, won, lost]
        turn: {type: integer, minimum: 1}
        seed: {type: string}
        rng_cursor: {type: integer, minimum: 0}
        player:
          $ref: "#/components/schemas/CombatPlayerState"
        enemies:
          type: array
          minItems: 1
          maxItems: 2
          items:
            $ref: "#/components/schemas/CombatEnemyState"
        draw_pile:
          type: array
          items: {$ref: "#/components/schemas/CardInstance"}
        discard_pile:
          type: array
          items: {$ref: "#/components/schemas/CardInstance"}
        hand:
          type: array
          maxItems: 10
          items: {$ref: "#/components/schemas/CardInstance"}
        exhaust_pile:
          type: array
          items: {$ref: "#/components/schemas/CardInstance"}
        played_types:
          type: array
          items: {type: string}
        previous_card_type:
          type: string
        cards_played: {type: integer, minimum: 0}
        route_completed: {type: boolean}
        next_card_sequence: {type: integer, minimum: 1}
        noise_level: {type: integer, minimum: 0, maximum: 3}
    MapNode:
      type: object
      additionalProperties: false
      required: [id, layer, lane, type, status, next]
      properties:
        id: {type: string}
        layer: {type: integer, minimum: 1, maximum: 6}
        lane: {type: integer, minimum: 0, maximum: 1}
        type:
          type: string
          enum: [combat, elite, event, story, rest, boss]
        status:
          type: string
          enum: [locked, available, current, completed]
        next:
          type: array
          items: {type: string}
        enemy_slugs:
          type: array
          items: {type: string}
        event_slug:
          type: string
    RunMap:
      type: object
      additionalProperties: false
      required: [nodes]
      properties:
        nodes:
          type: array
          items: {$ref: "#/components/schemas/MapNode"}
        current_node_id:
          type: string
    RewardState:
      type: object
      additionalProperties: false
      required: [card_choices]
      properties:
        card_choices:
          type: array
          maxItems: 3
          items: {type: string}
        granted_relic:
          type: string
    RunState:
      type: object
      additionalProperties: false
      required: [phase, chapter_slug, character_slug, noise_level, health, max_health, deck, relics, map, choice_tags, next_card_sequence, rng_cursor]
      properties:
        phase:
          type: string
          enum: [map, combat, reward, event, rest, completed]
        chapter_slug: {type: string}
        character_slug: {type: string}
        noise_level: {type: integer, minimum: 0, maximum: 3}
        health: {type: integer, minimum: 0}
        max_health: {type: integer, minimum: 1}
        deck:
          type: array
          minItems: 7
          items: {$ref: "#/components/schemas/CardInstance"}
        relics:
          type: array
          items: {type: string}
        map:
          $ref: "#/components/schemas/RunMap"
        combat:
          $ref: "#/components/schemas/CombatStateV2"
        reward:
          $ref: "#/components/schemas/RewardState"
        current_event_slug:
          type: string
        choice_tags:
          type: array
          items: {type: string}
        next_card_sequence: {type: integer, minimum: 1}
        rng_cursor: {type: integer, minimum: 0}
    GameRun:
      type: object
      additionalProperties: false
      required: [id, content_version, state, status, outcome, version, created_at, updated_at, completed_at]
      properties:
        id:
          type: string
          format: uuid
        content_version: {type: string}
        state:
          $ref: "#/components/schemas/RunState"
        status:
          type: string
          enum: [active, completed, abandoned]
        outcome:
          type: [string, "null"]
          enum: [cleared, failed, abandoned, null]
        version: {type: integer, minimum: 1}
        created_at: {type: string, format: date-time}
        updated_at: {type: string, format: date-time}
        completed_at: {type: [string, "null"], format: date-time}
    GameSnapshot:
      type: object
      additionalProperties: false
      required: [player, progress, active_run, pending_scene_slug]
      properties:
        player:
          $ref: "#/components/schemas/GamePlayer"
        progress:
          $ref: "#/components/schemas/GameProgress"
        active_run:
          oneOf:
            - $ref: "#/components/schemas/GameRun"
            - type: "null"
        pending_scene_slug:
          type: [string, "null"]
    CreateRunRequest:
      type: object
      additionalProperties: false
      required: [chapter_slug, character_slug, noise_level]
      properties:
        chapter_slug: {type: string}
        character_slug: {type: string}
        noise_level: {type: integer, minimum: 0, maximum: 3}
    RunCommandType:
      type: string
      enum: [choose_node, play_card, end_turn, choose_card_reward, resolve_event, rest, abandon_run]
    RunCommandRequest:
      type: object
      additionalProperties: false
      required: [type, expected_version]
      properties:
        type:
          $ref: "#/components/schemas/RunCommandType"
        expected_version: {type: integer, minimum: 1}
        node_id: {type: string}
        card_instance_id: {type: string}
        target_id: {type: string}
        choice_slug: {type: string}
        operation:
          type: string
          enum: [heal, remove]
    CombatEventV2:
      type: object
      additionalProperties: false
      required: [kind]
      properties:
        kind: {type: string}
        actor: {type: string}
        target_id: {type: string}
        card_slug: {type: string}
        intent_slug: {type: string}
        amount: {type: integer}
    RunEvent:
      type: object
      additionalProperties: false
      required: [kind]
      properties:
        kind: {type: string}
        node_id: {type: string}
        card_slug: {type: string}
        relic_slug: {type: string}
        choice_tag: {type: string}
        amount: {type: integer}
        combat:
          $ref: "#/components/schemas/CombatEventV2"
    RunCommandResponse:
      type: object
      additionalProperties: false
      required: [run, events]
      properties:
        run:
          $ref: "#/components/schemas/GameRun"
        events:
          type: array
          items: {$ref: "#/components/schemas/RunEvent"}
    StoryChoiceRequest:
      type: object
      additionalProperties: false
      required: [scene_slug, option_slug, expected_version]
      properties:
        scene_slug: {type: string}
        option_slug: {type: string}
        expected_version: {type: integer, minimum: 1}
    StoryChoiceResponse:
      type: object
      additionalProperties: false
      required: [progress, pending_scene_slug]
      properties:
        progress:
          $ref: "#/components/schemas/GameProgress"
        pending_scene_slug:
          type: [string, "null"]
