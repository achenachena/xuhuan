# Admin API Configuration
# Copy this file to .env.admin and fill in your values
# .env.admin is gitignored for security

# Railway Production API URL
API_URL=https://xuhuanbackend-production.up.railway.app

# Admin token for character management (must match Railway's CHARACTER_ADMIN_TOKEN)
# Generate with: openssl rand -hex 32
ADMIN_TOKEN=your_admin_token_here
