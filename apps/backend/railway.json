{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace packages/game-types && npm run db:generate --workspace apps/backend && npm run build --workspace apps/backend"
  },
  "deploy": {
    "startCommand": "npm run start --workspace apps/backend",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}

